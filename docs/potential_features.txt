Real access control: replace the placeholder password with proper auth so you can safely deploy the admin.

Backup/export safety: one-click download of posts.json and admin/data.json (plus images manifest), and an easy restore path.

Post “broadcast” controls: keep the RSS/social toggle you just added, plus a clear draft/published state so you can stage posts without accidental pushes.

Health/quality checks: a quick broken-link/image check for posts and chapters to catch bad URLs before publishing.

After those, scheduling and richer previews/analytics are nice to have but not as urgent.